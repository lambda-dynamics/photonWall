<h1>Editing Slide</h1>

<%= render 'form', slide: @slide %>

<%= link_to 'Show', @slide %> |
	<%= link_to 'Delete', @slide, method: :delete, data: { confirm: 'Are you sure?' }, class: 'button card-footer-item is-danger' %> |
<%= link_to 'Back', slides_path %>
